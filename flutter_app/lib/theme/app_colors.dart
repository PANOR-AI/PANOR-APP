import 'package:flutter/material.dart';

class AppColors {
  // Common Colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color border = Color(0xFFE2E8F0);

  // Patient Colors (Blue)
  static const Color patientPrimary = Color(0xFF0066FF);
  static const Color patientLight = Color(0xFFEFF6FF);

  // Doctor Colors (Green)
  static const Color doctorPrimary = Color(0xFF00C853);
  static const Color doctorLight = Color(0xFFF0FDF4);

  // Admin Colors (Purple)
  static const Color adminPrimary = Color(0xFF7C3AED);
  static const Color adminLight = Color(0xFFF5F3FF);
}
