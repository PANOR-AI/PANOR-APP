import 'package:flutter/material.dart';

class OnboardingScreen5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Onboarding 5'),
      ),
      body: Center(
        child: Text('Welcome to Onboarding Screen 5!'),
      ),
    );
  }
}
